/**
 * Sticky top bar is ~4rem; react-scroll offset clears the anchor under the bar.
 * Adjust if Navbar height changes.
 */
export const SCROLL_OFFSET = -80;

/** Applied to <section> for in-page link targets (matches sticky nav height). */
export const SCROLL_MARGIN_CLASS = 'scroll-mt-24';
